import 'package:cis_menu/utils/empty_page.dart';
import 'package:flutter/material.dart';

class RekonTransaksiPage extends StatelessWidget {
  const RekonTransaksiPage({super.key});

  @override
  Widget build(BuildContext context) => const EmptyPage(title: 'Rekon Transaksi');
}
